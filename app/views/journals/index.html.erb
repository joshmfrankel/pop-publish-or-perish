<%= javascript_pack_tag 'journals' %>

<%
  data = {
    journals: @journals,
    token: session[:_csrf_token],
    user: {
      is_admin: current_user.admin_role?
    }
  }
%>
<%= content_tag :div,
  id: "journals_listing",
  data: data.to_json do %>
<% end %>

<%= link_to 'Create Journal', new_journal_path %>
